import { faPizzaSlice } from '@fortawesome/free-solid-svg-icons'
import FeatureCard from './FeatureCard'

export default function PublisherFeatures() {
  return (
    <>
      <FeatureCard icon={faPizzaSlice} title='contacts' text='contacts' />
      <FeatureCard icon={faPizzaSlice} title='contacts' text='contacts' />
      <FeatureCard icon={faPizzaSlice} title='contacts' text='contacts' />
      <FeatureCard icon={faPizzaSlice} title='contacts' text='contacts' />
      <FeatureCard icon={faPizzaSlice} title='contacts' text='contacts' />
    </>
  )
}
