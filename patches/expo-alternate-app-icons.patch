diff --git a/build/index.d.ts b/build/index.d.ts
index 00650788cb26f5adfca8be5e63a030eac4fa0af2..77175729f9ce891aaaecb119c01794c1a74e10a4 100644
--- a/build/index.d.ts
+++ b/build/index.d.ts
@@ -28,7 +28,15 @@ export declare const supportsAlternateIcons: boolean;
  * // Reset to the default app icon.
  * const resetResult = await setAlternateAppIcon(null);
  */
-export declare function setAlternateAppIcon(name: AlternateAppIcons | null): Promise<string | null>;
+export type SetAlternateAppIconOptions = {
+    /**
+     * When true, attempts to suppress the iOS "App Icon Updated" system alert
+     * via a private selector. Falls back to the public API (alert shown) when
+     * the selector is unavailable.
+     */
+    silent?: boolean;
+};
+export declare function setAlternateAppIcon(name: AlternateAppIcons | null, options?: SetAlternateAppIconOptions): Promise<string | null>;
 /**
  * Retrieves the name of the currently active app icon.
  *
diff --git a/build/index.js b/build/index.js
index b7e31bfeb8e5761f63922322402cd4fc0747ee0d..c546aeeb115beb19be0ef2529736a7c501d9ad48 100644
--- a/build/index.js
+++ b/build/index.js
@@ -27,8 +27,9 @@ export const supportsAlternateIcons = ExpoAlternateAppIconsModule.supportsAltern
  * // Reset to the default app icon.
  * const resetResult = await setAlternateAppIcon(null);
  */
-export async function setAlternateAppIcon(name) {
-    return ExpoAlternateAppIconsModule.setAlternateAppIcon(name);
+export async function setAlternateAppIcon(name, options) {
+    const silent = options?.silent ?? false;
+    return ExpoAlternateAppIconsModule.setAlternateAppIcon(name, silent);
 }
 /**
  * Retrieves the name of the currently active app icon.
diff --git a/ios/ExpoAlternateAppIconsModule.swift b/ios/ExpoAlternateAppIconsModule.swift
index 8281c53c5fe7404e4731e7b92ff55c7273823c0f..5e59dbd4549423904268f6c24bceed04da746360 100644
--- a/ios/ExpoAlternateAppIconsModule.swift
+++ b/ios/ExpoAlternateAppIconsModule.swift
@@ -20,8 +20,28 @@ public class ExpoAlternateAppIconsModule: Module {
         return UIApplication.shared.alternateIconName;
     }
 
-    private func setAlternateAppIcon(icon: String?, promise: Promise) -> Void {
+    // PATCHED (witness-work): accepts a `silent` flag. When true, calls the
+    // private `_setAlternateIconName:completionHandler:` selector to suppress
+    // the system "App Icon Updated" alert. Falls back to the public API if
+    // Apple ever removes the selector — worst case, the alert reappears.
+    private func setAlternateAppIcon(icon: String?, silent: Bool, promise: Promise) -> Void {
         Task { @MainActor in
+            if silent {
+                let selector = NSSelectorFromString("_setAlternateIconName:completionHandler:")
+                if UIApplication.shared.responds(to: selector) {
+                    typealias SilentSetIconFn = @convention(c) (NSObject, Selector, NSString?, @escaping (NSError?) -> Void) -> Void
+                    let imp = UIApplication.shared.method(for: selector)
+                    let fn = unsafeBitCast(imp, to: SilentSetIconFn.self)
+                    fn(UIApplication.shared, selector, icon as NSString?) { error in
+                        if let error = error {
+                            promise.reject(error)
+                        } else {
+                            promise.resolve(icon)
+                        }
+                    }
+                    return
+                }
+            }
             do {
                 try await UIApplication.shared.setAlternateIconName(icon);
 
diff --git a/src/index.ts b/src/index.ts
index bd118ab37bd5cf88f16116c570265bfcc004cbfa..f4beb69fc448233d7b66f4c140046aca36b3ca6d 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -33,8 +33,21 @@ export const supportsAlternateIcons: boolean = ExpoAlternateAppIconsModule.suppo
  * const resetResult = await setAlternateAppIcon(null);
  */
 
-export async function setAlternateAppIcon(name: AlternateAppIcons | null): Promise<string | null> {
-  return ExpoAlternateAppIconsModule.setAlternateAppIcon(name);
+export type SetAlternateAppIconOptions = {
+  /**
+   * When true, attempts to suppress the iOS "App Icon Updated" system alert
+   * via a private selector. Falls back to the public API (alert shown) when
+   * the selector is unavailable.
+   */
+  silent?: boolean;
+};
+
+export async function setAlternateAppIcon(
+  name: AlternateAppIcons | null,
+  options?: SetAlternateAppIconOptions
+): Promise<string | null> {
+  const silent = options?.silent ?? false;
+  return ExpoAlternateAppIconsModule.setAlternateAppIcon(name, silent);
 }
 
 /**
